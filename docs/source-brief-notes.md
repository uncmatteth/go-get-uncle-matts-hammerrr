# Source Brief Notes

The source brief described HAMMERRR as:

- "Go Get Uncle Matt's HAMMERRR"
- "Hardcore Audit of Misleading Models, Evidence Reliability, Runtime & Reality"
- "When the repo's story stops matching the system, go get Uncle Matt's HAMMERRR."
- "A read-only project-truth audit. HAMMERRR investigates before anyone starts swinging."

Implementation choices made for this repo:

- Use the standards-safe slug `go-get-uncle-matts-hammerrr`.
- Keep the public display name in README, `skill-card.md`, and `agents/openai.yaml`.
- Keep Codex-style skill frontmatter to `name` and `description`.
- Publish as a skill package, not a plugin wrapper.
- Include only deterministic local validation, not placeholder LLM benchmark scripts.
